var num_elements = 14.0;

var num_elements_r1 = Math.round(num_elements/3.0);
var num_elements_r2 = Math.round(num_elements/3.0);
var num_elements_r3 = Math.round(num_elements/3.0);
// var num_elements_r4 = num_elements - num_elements_r1 - num_elements_r2 - num_elements_r3;


// for (var i=0; i < num_elements_r1; i++) {
//     var pos = (100.0/num_elements_r1) * i;
//     $("#member" + (i+1)).css({left: pos + "%"});
// }


// for (var i=0; i < num_elements_r2; i++) {
//     var pos = (100.0/num_elements_r2) * i;
//     $("#member" + (i+1+num_elements_r1)).css({left: pos + "%"});
// }

// for (var i=0; i < num_elements_r3; i++) {
//     var pos = (100.0/num_elements_r3) * i;
//     $("#member" + (i+1+num_elements_r2+num_elements_r1)).css({left: pos + "%"});
// }

// for (var i=0; i < num_elements_r4; i++) {
//     var pos = (100.0/num_elements_r4) * i;
//     $("#member" + (i+1+num_elements_r3+num_elements_r2+num_elements_r1)).css({left: pos + "%"});
// }

/*
for (var i=0; i < num_elements_r1; i++) {
    var pos = (100.0/num_elements_r1) * i;
    $("#member" + (i+1)).css({left: pos + "%"});
    $("#overlay" + (i+1)).css({left: pos + "%"});
}


for (var i=0; i < num_elements_r2; i++) {
    var pos = (100.0/num_elements_r2) * i;
    $("#member" + (i+1+num_elements_r1)).css({left: pos + "%"});
    $("#overlay" + (i+1+num_elements_r1)).css({left: pos + "%"});
}

for (var i=0; i < num_elements_r3; i++) {
    var pos = (100.0/num_elements_r3) * (i + 0.5) ;
    $("#member" + (i+1+num_elements_r2+num_elements_r1)).css({left: pos + "%"});
    $("#overlay" + (i+1+num_elements_r1+num_elements_r2)).css({left: pos + "%"});
}

// for (var i=0; i < num_elements_r4; i++) {
//     var pos = (100.0/num_elements_r4) * i;
//     $("#overlay" + (i+1+num_elements_r3+num_elements_r1+num_elements_r2)).css({left: pos + "%"});
// }
*/